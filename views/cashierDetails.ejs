<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">

<link rel="stylesheet" type="text/css" href="https://netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet">
<link rel="stylesheet" href="/css/style_main.css">
<title> Supermarket Inner Page </title>
</head>
<header>
  
  <h1><span>Welcome </span><%=  user.firstName%> <%= user.lastName%></h1>
  <h3>Cashier Details Page</h3>

</header>
<div class="flex">
<nav>
  <a class="logo" href="#">
    <h2>Activites</h2>
    <p>
      <% if(user.positionID == "4") { %>
        Privilege Level Admin 
        <% } %>
        <% if(user.positionID == "3") { %>
          Privilege Level Receiver 
          <% } %>
          <% if(user.positionID == "2") { %>
            Privilege Level Price Coordinator 
            <% } %>
            <% if(user.positionID == "1") { %>
              Privilege Level Cashier 
              <% } %>
      </p>
  </a> 
 
  <a href="/">
    <i class="fa fa-book fa-lg"></i>
    <span>Home</span>
  </a>
  <% if(user.positionID == "4") { %>

    <form id="users" action = "/users" method="get">
      <button type="submit"><i class="fa fa-user fa-lg"></i><span>Users</span></button>
      </form>
      <form id="inventory" action = "/addInventory" method="get">
        <button type="submit"><i class="fa fa-book fa-lg"></i><span>Add Inventory</span></button>
        </form>
        <form id="delivery" action = "/delivery" method="get">
          <button type="submit"><i class="fa fa-book fa-lg"></i><span>Delivery</span></button>
          </form>
          <form id="inventory" action = "/inventory" method="get">
            <button type="submit"><i class="fa fa-book fa-lg"></i><span>View Inventory</span></button>
            </form>
            <form id="inventory" action = "/transactionDetails" method="get">
              <button type="submit"><i class="fa fa-book fa-lg"></i><span>CashierPage</span></button>
              </form>
      
    <% } %>
    <% if(user.positionID == "3") { %>
      <form id="inventory" action = "/addInventory" method="get">
        <button type="submit"><i class="fa fa-book fa-lg"></i><span>Add Inventory</span></button>
        </form>
        <form id="delivery" action = "/delivery" method="get">
          <button type="submit"><i class="fa fa-book fa-lg"></i><span>Delivery</span></button>
          </form>

      <% } %>

      <% if(user.positionID == "2") { %>
        <form id="inventory" action = "/inventory" method="get">
          <button type="submit"><i class="fa fa-book fa-lg"></i><span>View Inventory</span></button>
          </form>
          <form id="inventory" action = "/addInventory" method="get">
            <button type="submit"><i class="fa fa-book fa-lg"></i><span>Add Inventory</span></button>
            </form>
  
        <% } %>
        <% if(user.positionID == "1") { %>
          <form id="inventory" action = "/transactionDetails" method="get">
            <button type="submit"><i class="fa fa-book fa-lg"></i><span>CashierPage</span></button>
            </form>
    
          <% } %>
  

    <form id="logoutf" action = "/logout?_method=DELETE" method="POST">
    <button type="submit"><i class="fa fa-key fa-lg"></i><span>Log Out</span></button>
    </form>
 

  </nav>
  <div class="contents">
    <h1>inventory</h1>

  
    <h3>List of Transaction Comleted by the User Including Customers </h3>
  
    <% if(errors != "") { %>
      <% errors.forEach((item) => { %>
        <br><br>  
        <span class = "alert alert-danger">
              <%- item %>
          </span>
      <% }); %>
  <% } %>

  <% if(data != "") { %>
    <% let i = 1 %>
    <br><br>
    <table id="cashierDetails"  >
      <tr>
        <th style="min-width:10mm">#</th> 
        
        <th style="min-width:20mm"> Transaction ID</th> 
        <th style="min-width:30mm"> Customer First Name</th>
        <th style="min-width:30mm"> Customer Last Name</th>
        <th style="min-width:20mm"> Payment ID</th>
        <th style="min-width:40mm"> Transaction Total</th> 
        <th style="min-width:30mm"> Payment Method</th>


      </tr>

    <% data.forEach((rows) => { %>
      <tr>
      <td style="min-width:10mm; text-align: center;"><%= i++ %></td> 
      <td style="min-width:20mm"><%= rows.transactionID %></td>
      <td style="min-width:20mm"><%= rows.firstName %></td>
      <td style="min-width:40mm"><%= rows.lastName %></td>
      <td style="min-width:20mm"><%= rows.paymentID %></td>
      <td style="min-width:40mm"><%= rows.transactionTotal %></td>
      <td style="min-width:30mm"> <%= rows.paymentMethod %></td>
      </tr>

    <% }); %>
    <tfoot>
      <tr>
        <td>Total For Transactions</td>
        <td><% if(result != "") { %> 
          <%= result %>
          
          <% } %></td>
      </tr>
    </tfoot>
  </table>
<% } %>
   





  </div>
</div>
<footer>
 <div class="left">
      Final Project Templete | some context| 
  </div>
  <div class="right">
    <a href="#"> Home</a>
    <a href="#">About</a>
    <a href="#">Contact</a>
    <a href="#">Privacy Policy</a>
  </div>
</footer>